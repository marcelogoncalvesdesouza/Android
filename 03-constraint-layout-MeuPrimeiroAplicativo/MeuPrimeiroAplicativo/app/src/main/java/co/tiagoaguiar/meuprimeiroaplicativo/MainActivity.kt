package co.tiagoaguiar.meuprimeiroaplicativo

import android.os.Bundle

import androidx.appcompat.app.AppCompatActivity


class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        

        //  mac | win/linux
        // (CMD | ALT) + 1 : explorador de projeto
        // (CMD | ALT) + 7 : estrutura da classe
        // (CMD | Ctrl) + click : abre o layout
        // (Option | Alt) + Enter : importar a classe
        // (CMD | ctrl) + E : abrir arquivos recentes
        // (CMD | ctrl) + F : pesquisar arquivos
        // find in path : pesquisar em qq arquivo

        // tecla mestre de atalho
        // SHIFT + SHIFT : faz tudo
    }

}